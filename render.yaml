services:
  - type: web
    plan: free
    name: repo-explainer
    runtime: python
    repo: https://github.com/BaoNguyen09/repo-explainer
    rootDir: backend
    buildCommand: 'pip install -r requirements.txt'
    startCommand: 'fastapi run --host 0.0.0.0 --port $PORT'